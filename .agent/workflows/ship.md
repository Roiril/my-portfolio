---
description: チェック、ビルド、コミット、プッシュを一括で実行する
---

1. `/check` を実行し、Lintエラーがないか確認する。失敗した場合は中断する。
2. `/build` を実行し、ビルドエラーがないか確認する。失敗した場合は中断する。
3. `/commit` を実行する。差分を確認し、メッセージを提案してからコミットを作成する。
4. `/push` を実行し、リモートに変更を反映する。
5. 全ての手順が成功したことを報告して終了する。途中で失敗した場合は、その箇所を明示して停止する。
